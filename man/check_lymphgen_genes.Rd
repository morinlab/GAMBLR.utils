% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lymphgen_to_dlbclone.R
\name{check_lymphgen_genes}
\alias{check_lymphgen_genes}
\title{Check gene symbols against a LymphGen feature matrix}
\usage{
check_lymphgen_genes(genes, feature_matrix)
}
\arguments{
\item{genes}{Character vector of HGNC gene symbols to validate.}

\item{feature_matrix}{Data frame returned by
\code{lymphgen_to_dlbclone()}. Mutation-related columns are those
lacking a \verb{_gain} or \verb{_loss} suffix and not named \code{sample_id}.
A gene is considered present if any column matches \verb{^GENE(_|$)}.}
}
\value{
A data frame (invisibly) with columns:
\describe{
\item{gene_requested}{Symbol as supplied by the user.}
\item{status}{\code{"found"}, \code{"alias"}, or
\code{"unknown"}.}
\item{suggested_symbol}{Current HGNC symbol when
\code{status == "alias"}; \code{NA} otherwise. Note: the suggested
symbol may itself not be present in \code{feature_matrix} if no
samples in the dataset carried that gene's mutation.}
}
}
\description{
Validate a set of HGNC gene symbols against the genes
available in a \code{lymphgen_to_dlbclone()} feature matrix, and
identify any outdated symbols using the \pkg{org.Hs.eg.db} alias table.
}
\details{
For each requested gene the function reports one of three statuses:
\itemize{
\item \strong{found} - the symbol matches at least one mutation-related column
in \code{feature_matrix}. Columns are matched by the pattern
\verb{^GENE(_|$)}, so \code{MYD88_hotspot}, \code{MYD88_other}, and plain \code{MYD88}
all count as a match for the gene \code{MYD88}.
\item \strong{alias} - the symbol is not a current HGNC name but is a known
alias; a suggested replacement is returned
(e.g. \code{HIST1H1C} -> \code{H1-2}).
\item \strong{unknown} - the symbol was not found as either a current HGNC
name or any known alias in \pkg{org.Hs.eg.db}.
}

The function always prints a one-line summary and, for \code{alias} and
\code{unknown} cases, a detailed breakdown. The return value is invisibly
returned so the function can be used for its side-effect messages or
captured for programmatic use.
}
\examples{
\dontrun{
mat <- lymphgen_to_dlbclone(
  mutation_flat_path     = "lymphgen_inputs/mutation_flat.txt",
  cnv_path               = "lymphgen_inputs/cnv_flat.txt",
  sample_annotation_path = "lymphgen_inputs/sample_annotation.txt"
)

check_lymphgen_genes(
  genes          = c("MYD88", "HIST1H1C", "NOTCH1", "FAKEGENE"),
  feature_matrix = mat
)
}

}
