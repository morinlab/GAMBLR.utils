% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lymphgen_to_dlbclone.R
\name{lymphgen_to_dlbclone}
\alias{lymphgen_to_dlbclone}
\title{Convert LymphGen inputs to a feature matrix compatible with DLBCLone}
\usage{
lymphgen_to_dlbclone(
  mutation_flat_path,
  cnv_path,
  sample_annotation_path,
  mutation_gene_list_path = NULL,
  genes = NULL,
  genes_hotspot = c("MYD88"),
  genes_noncoding = NULL,
  genes_synonymous = NULL,
  per_gene_type_filter = list(NOTCH1 = c("TRUNC"), NOTCH2 = c("TRUNC")),
  mutation_encoding = list(none = 0, synonymous = 1, coding = 2, truncating = 2, hotspot
    = 2),
  cnv_gain_encoding = list(none = 0, gain = 1, amp = 2),
  cnv_loss_encoding = list(none = 0, hetloss = 1, homdel = 2),
  sv_encoding = list(negative = 0, positive = 1),
  restrict_to_gene_list = TRUE,
  include_cnv = TRUE,
  include_sv = TRUE,
  genome_build = "grch37",
  genes_coordinate_filter = c("NOTCH1", "NOTCH2"),
  verbose = FALSE
)
}
\arguments{
\item{mutation_flat_path}{Path to the LymphGen mutation flat file
(tab-separated; columns: Sample, ENTREZ.ID, Type, Location).}

\item{cnv_path}{Path to the LymphGen copy-number file
(tab-separated; columns: Sample, ENTREZ.ID, Type).}

\item{sample_annotation_path}{Path to the LymphGen sample annotation
file (tab-separated; must contain \code{Sample.ID}; translocation columns
are detected automatically by the \code{.transloc} suffix).}

\item{mutation_gene_list_path}{Optional path to the LymphGen gene list
file (single column of ENTREZ IDs, one per row, with a header). When
supplied with \code{restrict_to_gene_list = TRUE}, only genes from this
list appear in the output.}

\item{genes}{Optional character vector of HGNC gene symbols. When
provided, the output matrix is subset to all columns belonging to
those genes (mutation, hotspot, other, noncoding, CNV, and SV
columns alike). Any symbol not found is checked via
\code{\link{check_lymphgen_genes}}, which messages the user with
alias suggestions for outdated names.}

\item{genes_hotspot}{Character vector of genes whose mutations should
be split into \verb{<GENE>_hotspot} (named hotspot types, e.g. L265P)
and \verb{<GENE>_other} (MUTATION / TRUNC) columns instead of a single
\verb{<GENE>} column. Default: \code{c("MYD88")}.}

\item{genes_noncoding}{Optional character vector of genes for which
\code{Synon} mutations are tracked in a dedicated \verb{<GENE>_noncoding}
column (modelling aSHM signal). For genes also in \code{genes_hotspot},
noncoding tracking is applied on top of the hotspot/other split.
Default: \code{NULL} (no noncoding columns).}

\item{genes_synonymous}{Optional character vector of genes for which
\code{Synon} mutations are included in the plain \verb{<GENE>} column (scored
with \code{mutation_encoding$synonymous}). For genes \strong{not} in this list
(and not in \code{genes_noncoding}), a sample with only a \code{Synon}
mutation scores 0, matching the behaviour of
\code{assemble_genetic_features()} where synonymous signal is only
carried for genes explicitly listed in \code{genes_noncoding}. This
parameter has no effect on genes in \code{genes_hotspot} (those never
produce a plain \verb{<GENE>} column). Default: \code{NULL}.}

\item{per_gene_type_filter}{Named list restricting which LymphGen
\code{Type} values are counted for specific genes. Each name is an HGNC
gene symbol; each value is a character vector of the \code{Type} strings
that are \strong{kept} (e.g. \code{c("TRUNC")} to accept truncating mutations
only). \code{Synon} rows are always exempt — they are governed by
\code{genes_noncoding} / \code{genes_synonymous} and are never removed by this
filter. Genes not present in the list are unrestricted. To disable
all defaults pass \code{list()}. Default encodes the two clearest
LymphGen rules:
\code{list(NOTCH1 = c("TRUNC"), NOTCH2 = c("TRUNC"))}.}

\item{mutation_encoding}{Named list controlling the numeric score for
each LymphGen mutation class. Recognised keys: \code{none}, \code{synonymous},
\code{coding}, \code{truncating}, \code{hotspot}. Default:
\code{list(none = 0, synonymous = 1, coding = 2, truncating = 2, hotspot = 2)}.}

\item{cnv_gain_encoding}{Named list controlling scores for copy gains.
Recognised keys: \code{none}, \code{gain}, \code{amp}.
Default: \code{list(none = 0, gain = 1, amp = 2)}.}

\item{cnv_loss_encoding}{Named list controlling scores for copy losses.
Recognised keys: \code{none}, \code{hetloss}, \code{homdel}.
Default: \code{list(none = 0, hetloss = 1, homdel = 2)}.}

\item{sv_encoding}{Named list controlling scores for translocation
status. Recognised keys: \code{negative}, \code{positive}.
Default: \code{list(negative = 0, positive = 1)}.}

\item{restrict_to_gene_list}{Logical. When \code{TRUE} (default) and
\code{mutation_gene_list_path} is provided, output columns are restricted
to genes present in that list.}

\item{include_cnv}{Logical. When \code{TRUE} (default), CNV gain/loss
columns are appended to the output.}

\item{include_sv}{Logical. When \code{TRUE} (default), SV/translocation
columns from the sample annotation file are appended to the output.}

\item{genome_build}{Reference genome build string used by the
coordinate-based driver filter and passed to
\code{\link{get_driver_coordinates}}. Supported values:
\code{"grch37"} (default), \code{"hg19"}, \code{"hs37d5"}, \code{"GRCh37"},
\code{"hg38"}, \code{"grch38"}, \code{"GRCh38"}. Ignored when
\code{genes_coordinate_filter} is \code{NULL} or empty.}

\item{genes_coordinate_filter}{Optional character vector of HGNC gene
symbols to filter by curated driver coordinates via
\code{\link{annotate_curated_drivers}}. Only mutations that fall
inside the defined driver region for each gene \strong{and} match the
expected variant class are retained; all others are dropped.
\code{Synon} rows are always exempt (governed by \code{genes_noncoding} /
\code{genes_synonymous}). Genes listed in \code{genes_hotspot} are silently
skipped (hotspot splitting already handles them). For genes handled
by this filter, any entry in \code{per_gene_type_filter} is automatically
skipped to avoid double-filtering. A warning is issued for any gene
not present in the curated coordinate table returned by
\code{\link{get_driver_coordinates}}. Set to \code{NULL} or
\code{character(0)} to disable coordinate filtering entirely and rely on
\code{per_gene_type_filter} alone. Default: \code{c("NOTCH1", "NOTCH2")},
which mirrors the coordinate restrictions LymphGen itself applies.}

\item{verbose}{Logical. When \code{TRUE}, progress messages are printed.}
}
\value{
A data frame with one row per sample and one column per
genomic feature. The first column is \code{sample_id}. Mutation columns
are named by HGNC symbol with optional \verb{_hotspot}, \verb{_other}, or
\verb{_noncoding} suffixes; CNV columns carry \verb{_gain} or \verb{_loss}; SV
columns carry \verb{_SV}. All values are numeric and reflect the supplied
encoding lists.
}
\description{
Read the standard LymphGen input files (mutation flat
file, copy-number file, and sample annotation file) and assemble them
into a wide sample-by-feature numeric matrix compatible with DLBCLone.
}
\details{
Four data types are extracted and combined:
\subsection{Mutations}{

Column naming mirrors the logic of \code{assemble_genetic_features()}:
\itemize{
\item \strong{Hotspot genes} (\code{genes_hotspot}, default \code{"MYD88"}) produce two
mutation columns per gene:
\itemize{
\item \verb{<GENE>_hotspot} - named hotspot mutations (any LymphGen
\code{Type} that is not \code{MUTATION}, \code{TRUNC}, or \code{Synon}, e.g. \code{L265P}).
\item \verb{<GENE>_other} - coding mutations outside the hotspot
(\code{MUTATION} or \code{TRUNC}).
}
\item \strong{Non-coding / aSHM genes} (\code{genes_noncoding}, default \code{NULL})
produce an additional \verb{<GENE>_noncoding} column for \code{Synon}
mutations. These genes also retain their regular mutation column(s).
\item \strong{All other genes} produce a single \verb{<GENE>} column. Only
non-synonymous mutations (\code{MUTATION}, \code{TRUNC}, named hotspots)
contribute to this column. \code{Synon} mutations are included only for
genes explicitly listed in \code{genes_synonymous}; for every other gene
a sample with only a \code{Synon} record scores 0.
}
}

\subsection{Per-gene mutation type filtering}{

\code{per_gene_type_filter} is a named list that restricts which LymphGen
\code{Type} values are kept for specific genes before any column assignment.
This mirrors LymphGen's own per-gene rules — e.g. NOTCH1/NOTCH2 count
only truncating mutations. \code{Synon} is always exempt from this filter
(it is governed independently by \code{genes_noncoding} / \code{genes_synonymous}).
Genes absent from the list are unrestricted.
}

\subsection{Copy-number variants}{

Each gene contributes two columns: \verb{<GENE>_gain} (GAIN / AMP) and
\verb{<GENE>_loss} (HETLOSS / HOMDEL). Default gain encoding: GAIN = 1,
AMP = 2. Default loss encoding: HETLOSS = 1, HOMDEL = 2.
}

\subsection{Structural variants / translocations}{

Any column ending in \code{.transloc} in the sample annotation file is
mapped to a \verb{<GENE>_SV} column (e.g. \code{BCL2.transloc} -> \code{BCL2_SV}).
}

\subsection{ENTREZ ID mapping}{

All ENTREZ IDs are converted to HGNC gene symbols via
\pkg{org.Hs.eg.db}. Genes that cannot be mapped are silently dropped.

When \code{genes} is provided the output is further subset. Any symbol not
found in the matrix is automatically checked via
\code{\link{check_lymphgen_genes}} so outdated names (e.g.
\code{HIST1H1C}) trigger a helpful suggestion.
}
}
\examples{
\dontrun{
# Full matrix; MYD88 is split into MYD88_hotspot + MYD88_other
feature_matrix <- lymphgen_to_dlbclone(
  mutation_flat_path      = "lymphgen_inputs/mutation_flat.txt",
  cnv_path                = "lymphgen_inputs/cnv_flat.txt",
  sample_annotation_path  = "lymphgen_inputs/sample_annotation.txt",
  mutation_gene_list_path = "lymphgen_inputs/mutation_gene_list.txt"
)

# Subset to a specific gene panel; includes _hotspot/_other for MYD88
dlbcl_panel <- lymphgen_to_dlbclone(
  mutation_flat_path      = "lymphgen_inputs/mutation_flat.txt",
  cnv_path                = "lymphgen_inputs/cnv_flat.txt",
  sample_annotation_path  = "lymphgen_inputs/sample_annotation.txt",
  mutation_gene_list_path = "lymphgen_inputs/mutation_gene_list.txt",
  genes = c("MYD88", "CD79B", "EZH2", "CREBBP", "BCL2", "TP53"),
  genes_noncoding = c("MYD88", "CREBBP", "PIM1", "SOCS1")
)
}

}
