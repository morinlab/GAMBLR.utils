% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_curated_hotspots.R
\name{summarise_mutation_status}
\alias{summarise_mutation_status}
\title{Summarize mutation status}
\usage{
summarise_mutation_status(
  annotated_maf,
  genes_coding,
  driver_report_genes = NULL,
  granular_report_genes = c("FOXO1", "CD79B")
)
}
\arguments{
\item{annotated_maf}{A data frame in MAF format that includes
\code{Hugo_Symbol}, \code{Tumor_Sample_Barcode}, \code{Variant_Classification},
\code{mutation_alias}, and \code{mutation_annotation}.}

\item{genes_coding}{Optional character vector of genes to include in the
coding summary. Defaults to Tier 1 lymphoma genes.}

\item{driver_report_genes}{Optional character vector of genes to include in
the driver summary. If NULL, genes with at least one \code{_driver}
annotation are used.}

\item{granular_report_genes}{Optional character vector of genes for which
to include granular hotspot aliases (default \code{c("FOXO1","CD79B")}).}
}
\value{
A wide data frame (matrix-like) with rows as samples and columns as
variant categories, containing 0/1 indicators.
}
\description{
Create a wide sample-by-variant matrix summarizing coding,
driver, and hotspot-level mutation status from an annotated MAF.
}
\details{
This function expects output from \code{annotate_curated_hotspots()}
(or equivalent) with \code{mutation_alias} and \code{mutation_annotation}
columns. It produces a wide matrix with 0/1 indicators for each
sample-variant combination, including per-gene coding status, driver status,
and granular hotspot aliases.
}
\examples{
\dontrun{
maf = GAMBLR.open::get_coding_ssm(
  these_samples_metadata = GAMBLR.open::get_gambl_metadata(),
  this_seq_type = "capture"
)
maf_anno = annotate_curated_hotspots(maf)
summary_mat = summarize_mutation_status(maf_anno)
summary_mat[1:5, 1:5]
}

\dontrun{
maf_anno = annotate_curated_hotspots(GAMBLR.open::get_coding_ssm())
summary_mat = summarize_mutation_status(
  maf_anno,
  genes_coding = c("MYD88","NOTCH1","FOXO1"),
  granular_report_genes = c("FOXO1")
)
}

}
